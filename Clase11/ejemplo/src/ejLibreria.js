// Importamos la librería moment
const moment = require('moment');

// Obtenemos la fecha y hora actual
const ahora = moment().format('YYYY-MM-DD HH:mm:ss');

// Mostramos en consola
console.log(`La fecha actual es: ${ahora}`);
